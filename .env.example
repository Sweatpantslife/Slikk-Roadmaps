# SQLite database (relative to prisma/ directory).
# Swap for a Postgres URL in production if you prefer — see README.
DATABASE_URL="file:./dev.db"

# Password the team uses to access /admin (status changes, changelog publishing).
# Pick something strong — anyone with this password can manage the board.
ADMIN_PASSWORD="change-me"
